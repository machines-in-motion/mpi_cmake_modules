Set if empty
============

## Introduction

Provide a simple macro to set a variable if it contains nothing.

## Usage

The following command affect value to varibale if variable is an empty one.
Otherwize it does nothing.

    set_if_empty(variable value)